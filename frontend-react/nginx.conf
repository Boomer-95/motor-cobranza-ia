server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: cualquier ruta no encontrada regresa a index.html (React Router, si lo usas)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
